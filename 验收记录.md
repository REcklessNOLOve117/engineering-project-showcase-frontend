# 前端验收记录

日期：2026-09-20。范围：本地静态站点 `dist/`，共六页。使用本地 HTTP 服务与 Codex 内置 Chromium 预览器检查。未重新运行模型训练或评测，案例指标依据用户提供的 PPT 核对。

## 最新首页分组

用户进一步明确后面三个项目均已落地。首页现按“紧凑介绍 → 三个已落地项目 → 精选落地案例 → 研发与评测”组织：制造业通用智能体、本地知识应用智能体、模型训练与微调并列呈现，训练项目也统一标记为“已落地案例”；世界模型与辅助编程在后面的独立区域展示。原三类能力目录和嵌套的行业案例分区已取消，`#industry-cases` 保留为新落地项目区的兼容锚点。

最新布局预览见 `artifacts/screenshots/home-stage-1440.png`、`home-stage-390.png`；之前的截图保留作版本参考。

重排后检查 1440、1024、768、390px 首页：落地项目数量为 3，三项状态均为“已落地案例”；研发与评测数量为 2；各宽度无横向溢出。移动导航可定位到新的研发与评测分区并自动收起。

## 字体与排版调整复验

同日根据“字体和排版换一下”的反馈更新六页：使用本地 Noto Sans SC / 思源黑体优先字体序列，降低标题字重；移除重复英文眉题、悬浮文案与大部分卡片外框；首页采用真实演示截图及横向项目目录。基础样式保留在 `styles.css`，当前排版规则集中在 `editorial.css`。

调整后重新检查全部六页在 1440、1024、768、390px 下的布局，均未发现横向溢出或图片加载失败。复验移动导航、标签页方向键及 End 键、图片预览、Esc 关闭与焦点恢复；结果正常。浏览器无控制台错误或警告，全部站内链接及引用文件通过本地资源检查。原生 200% 浏览器缩放的工具限制仍如下面记录所述。

新版截图以 `artifacts/screenshots/home-refined-1440.png`、`home-refined-390.png`、`projects-refined-1440.png`、`training-refined-1440.png` 为准。旧版截图保留作版本参考。

## 页面、素材与链接

- 首页和五个详情页均可直接导航、刷新、返回；五个详情标识与计划一致。
- 检查全部站内页面、链接片段及被引用的本地文件，共 16 个本地资源均返回 HTTP 200。
- 未发现失效站内链接、重复 ID、缺失页面标题或描述、缺失图片 `alt`。
- 页面、脚本、样式、截图和图标均在本地；没有外部字体、CDN 或自动加载的模型服务请求。
- 两个原站演示通过 `data-demo` 配置挂接，采用新标签页并设置 `noopener noreferrer`；辅助编程 HTTP 地址未嵌入 iframe。
- 外站当前运行环境不作为本站可用性的前提。页面保存真实界面截图，所有案例介绍可独立阅读。

## 布局与响应式

| 视口宽度 | 检查页面数 | 横向溢出 | 图片加载 |
| --- | --- | --- | --- |
| 1440px | 6 | 无 | 正常 |
| 1024px | 6 | 无 | 正常 |
| 768px | 6 | 无 | 正常 |
| 390px | 6 | 无 | 正常 |
| 720px 半宽重排 | 6 | 无 | 正常 |

桌面及平板首页、训练详情、世界模型详情、手机首页和手机详情已进行截图检查。修正了项目缩略图尺寸、方形概念插图裁切、锚点重复偏移及手机路线标签排列。图表柱高度按 100 分制的 65.94 和 81.975 等比例绘制，并保留文字与数值。

**200% 缩放限制：**内置预览器没有可设置浏览器缩放比例的接口，缩放快捷键也未改变页面缩放。因此只完成了 1440px 桌面在 200% 缩放时所对应的 720px CSS 视口重排检查，不能把它记为原生浏览器 200% 缩放已通过；仍需在常规浏览器中做一次原生缩放复核。

## 交互与键盘

- 首页“浏览项目”“查看精选案例”锚点定位正常，移动导航选择链接后自动收起。
- 移动导航按钮可打开、关闭，Esc 收起后焦点回到按钮。
- 世界模型四路线切换后只显示对应面板，`aria-selected`、Tab 顺序与阶段同步更新。
- OpenSora / WAN 显示“验证演示”，且正文说明尚未正式交付；LingbotVA 为“研发中”，Cosmos 为“引入中”。
- 标签页支持左右方向键及 Home / End；实测方向键可切换选中项并同步键盘焦点。
- 本地截图弹层可打开，关闭按钮和 Esc 均可关闭；关闭后恢复到原图片按钮，解除页面滚动锁定。
- 手机目录展开后可跳转到内容并自动收起；实测 Enter 导航后焦点转移到目标标题。
- 页面使用可见焦点样式、原生 dialog、语义标题与导航。动态效果受 `prefers-reduced-motion` 约束，无自动轮播或自动播放。

## 内容核对

- 首页指标仅归属“高端装备制造行业模型训练与优化”案例。
- 65.94 → 81.975：来源于 PPT 第 10 页图表缓存及备注，原始差值 16.035，约 16.0 分。
- 88.2%：样本得分提高至少 5 分的比例；与领域等权的宏平均提升约 16.1 分、Good Rate 约 85.4% 分开说明。
- 32 小时 → 3.5 小时：PPT 第 6 页的项目环境下单轮训练验证记录。
- 多模态输入属于下一步规划；世界模型耗时属于一次已保存记录；编程页没有引用占位耗时或合并不同条件下的榜单。
- 用户确认制造业通用智能体、本地知识应用智能体、模型训练与微调三个项目均已落地。三项统一标记为“已落地案例”，首页单独设置“已落地项目”区，相关项目标签与训练详情同步更新；保留流程示意标注，没有补写未确认的客户名称、技术机制或业务指标。

## 技术检查与交付

- `node --check` 检查共享交互脚本及演示配置语法通过。
- 浏览器检查中没有发现 JavaScript 控制台错误或警告；本地资源请求正常。
- 正文直接写入 HTML；世界模型所有路线正文保存在 HTML 中，无 JavaScript 时按顺序显示。
- `dist/` 为完整交付源文件，没有构建步骤。使用相对路径，适用于普通静态服务器及站点子目录。
- `preview.ps1` 提供本地预览入口，`README.md` 说明内容、图片和演示地址的维护方式。
- 压缩交付包 `ai-project-portal-static.zip` 仅包含 `dist/` 内的网站文件，不包含调研资料或原始 PPT。

关键预览：`artifacts/screenshots/home-1440.png`、`home-full-1440.png`、`home-390.png`、`training-results-1440.png`、`world-model-1440.png`、`world-model-390.png`。
